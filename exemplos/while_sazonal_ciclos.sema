// Exemplo: While sazonal por ciclos simulados (contador)
// Teste: make; ./semaforos; carregar exemplos/while_sazonal_ciclos.sema

ciclo = 0;
while (ciclo < 3) {
    mudar(verde);
    esperar(6);
    mudar(amarelo);
    esperar(2);
    mudar(vermelho);
    esperar(6);
    ciclo = ciclo + 1;
}


